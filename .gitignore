__pycache__/
*.pyc
*.pyo
*.log
results/
venv/
calibr_photo_1/

# Крупные бинарные данные - держим на диске, не в git (репозиторий уже ~815 МБ)
*.rar
archive/
calib2/
v6 (27.07.26)/
scratch_*.png
forms/
*.MOV

# рабочая папка входа и её бэкап — меняются на каждой съёмке
service_5056/input/_photos_current_backup/
05082026_test1/

# Сырьё съёмок — держим на диске, не в git
Helmet (10.08)/

# Сырьё пробы с лазерной точкой (180 МБ raw + фото пульта); извлечённые координаты
# лежат в laserdot_1/positions.csv и версионируются отдельно
laserdot_1/*
!laserdot_1/positions.csv

# То же для маркерных точек 18.08: .raw дублируется .png (конвертация без потерь,
# им и пользуются скрипты) - raw не нужен в истории
laserdot_2/*.raw

# Промежуточные картинки экспериментов - генерируются скриптами рядом
service_5056/scratch/**/*_contact.png
service_5056/scratch/**/t2_masks/
service_5056/scratch/**/masks/

# Дубликат отслеживаемого service_5056/input/model_3d/ (тот же файл побайтово)
current_3d/

# PNG, разворачиваемые из .raw скриптами рядом - не исходник
service_5056/scratch/phys/shop_png/

# Визуализации, которые строят сами скрипты в scratch (разметка, вырезки, наложения).
# Держим в репозитории код, а не его вывод: картинки восстанавливаются запуском.
service_5056/scratch/**/L1_out/
service_5056/scratch/**/L2_out/
service_5056/scratch/new0810/*.png

# Разметка эталонных линий - данные сессии, а не код (сервис 3030 создаёт их сам)
service_3030/data/lines/*.json
